#!/usr/bin/env bun

/**
 * Hoox Trading System TUI (Pane Layout)
 * 
 * This script starts the pane-based terminal user interface.
 */

// Import and run the TUI application
import './src/tui/app_panes.jsx';

// This script will exit when the TUI application exits 